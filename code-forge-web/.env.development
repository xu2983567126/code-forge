# 后端统一入口。开发期用「相对路径 /api」→ 走 vite.config.ts 的代理转发到网关 8101。
# 浏览器视角这是**同源**请求：cookie 自动携带，不受 SameSite / CORS 约束。
#
# ⚠️ 不要写成绝对地址（如 http://localhost:8101/api）。那样浏览器会直连网关成为
#    **跨源**请求，而 axios 默认 withCredentials:false → 跨源响应下发的 Set-Cookie
#    会被浏览器直接丢弃，表现为「登录接口 200 成功，但紧接着取登录态报未登录」。
#    （2026-09-18 实际踩到过，详见 src/config/api-client.ts 顶部注释。）
#
# 生产环境填**同源**网关地址，如 https://oj.example.com/api
VITE_API_BASE_URL=/api
